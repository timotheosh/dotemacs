(use-package web-search
  ;; We search. Use C-u M-x web-search for a list of search engines.
  :ensure t)

(use-package sos
  ;; Stackoverflow package. M-x sos to use.
  :ensure t)

(provide 'web-search-init)
